# agent-harness demo video

A standalone Remotion project for the `agent-harness` product demo video.

The video positions `agent-harness` as a reviewable lifecycle for reusable AI-agent assets: discover demand, load sources, build a catalog, rank recommendations, mirror assets, stage project-local files, activate host views, and wire supported editors only after review.

## Current artifact

The rendered video is generated locally at:

```console
out/agent-harness-product-demo.mp4
```

`out/` is intentionally ignored by Git so the repository stays source-focused.

## Story arc

1. Introduce `agent-harness` as an agent asset lifecycle for real repositories.
2. Show the lifecycle: demand → sources → catalog → recommend → mirror → stage → activate → wire.
3. Show the CLI surface and OpenCode workspace flow.
4. Show inspectable outputs generated by the lifecycle.
5. Make trust boundaries explicit.
6. Close with supported hosts and a community-feedback call.

## Commands

```console
npm install
npm run dev
npm run lint
npm run render
```

## Source layout

- `src/story.ts` — script, scene data, host adapter list, output examples.
- `src/Composition.tsx` — Remotion composition and visual scenes.
- `public/voiceover/` — generated narration takes.
- `public/audio/` — original generated music bed and attribution.
- `public/brand/hosts/` — host marks/wordmarks and attribution.
- `scripts/make-music-bed.mjs` — deterministic local music-bed generator.

## QA status

Latest local QA completed on 2026-05-30:

- `npm run lint` passed.
- Fresh Remotion render completed.
- `ffprobe` verified 1920×1080 H.264 video, 30fps, 2700 frames, stereo AAC audio, 90.048s duration.
- Sampled-frame visual QA found no broken final-render state.

## Attribution

See:

- `public/audio/ATTRIBUTION.md`
- `public/brand/hosts/ATTRIBUTION.md`

## License

See `LICENSE.md`.

Code is MIT licensed. Demo copy, generated voiceover/music, rendered video outputs, and third-party marks have separate restrictions described in the license file.
